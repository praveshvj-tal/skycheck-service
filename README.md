# skycheck-service
SkyCheck is a backend digital check-in system for airline passengers designed to safely handle high-traffic seat reservations. The system manages seat lifecycle states, enforces conflict-free seat assignments under concurrent access, supports temporary seat holds with automatic expiration, validates baggage rules, and simulates payment handling
